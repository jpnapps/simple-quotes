name: Play Store Internal Release (Manual)

on:
  workflow_dispatch:   # 👈 Manual run only

jobs:
  internal-release:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout source code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Java (Android requires JDK)
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 3️⃣ Decode keystore (original name)
      - name: Decode keystore
        run: |
           echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > app/simple_quotes.jks
           ls -lh app/simple_quotes.jks

      # 4️⃣ Create keystore.properties
      #- name: Create keystore properties
        #run: |
          #echo "storeFile=simple_quotes.jks" >> keystore.properties
          #echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" >> keystore.properties
          #echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> keystore.properties
          #echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> keystore.properties

      # 5️⃣ Grant Gradle permission
      - name: Grant execute permission
        run: chmod +x gradlew

      # 6️⃣ Build Release AAB
      - name: Build Release Bundle
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew bundleRelease

      # 7️⃣ Upload to Play Store (Internal Testing)
      - name: Upload to Play Store - Internal
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.jpdev.simplequotes   # 🔴 replace this
          releaseFiles: app/build/outputs/bundle/release/app-release.aab
          track: internal
          status: draft
